server:
  port: 8080
  shutdown_timeout: 30s

database:
  host: "postgres"
  port: 5432
  user: "postgres"
  password: "password"
  name: "subscriptions"
  sslmode: "disable"

log:
  level: "info"
  format: "text"
  output: "stdout"